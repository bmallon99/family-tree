{
  "name": "family-tree",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "dev": "concurrently \"tsc --watch\" \"live-server . --watch=dist,index.html,data --ignore=node_modules\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "live-server": "^1.2.2",
    "typescript": "^5.4.0"
  }
}
